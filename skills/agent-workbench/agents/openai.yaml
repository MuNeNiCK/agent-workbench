interface:
  display_name: "Agent Workbench"
  short_description: "Structured memory for coding-agent work"
  default_prompt: "Use $agent-workbench and the native agent-workbench executable to recover the explicit state file, follow its next action, and manage design, review, evidence, interruption, and completion for this project."

policy:
  allow_implicit_invocation: true
